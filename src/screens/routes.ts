const routes: { readonly [key: string]: string } = {
  FEED: "(tabs)/(feed)",
  LISTING_DETAILS: "(tabs)/(feed)/listingDetails",
  LISTING_EDIT: "listingEdit",
  LOGIN: "login",
  MESSAGES: "(tabs)/(account)/messages",
  REGISTER: "register",
};

export default routes;
